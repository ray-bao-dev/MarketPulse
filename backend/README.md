# API service (FastAPI)

| Setting | Value |
|---|---|
| Root Directory | `backend` |
| Dockerfile path | `Dockerfile` |

Required variables: `DATABASE_URL`, `ALPACA_API_KEY`, `ALPACA_API_SECRET`, `CORS_ORIGINS`.

Runs Alembic migrations on startup.
