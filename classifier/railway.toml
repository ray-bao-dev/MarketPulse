[build]
builder = "DOCKERFILE"
dockerfilePath = "classifier/Dockerfile"
watchPatterns = ["classifier/**", "backend/marketpulse/**"]

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
