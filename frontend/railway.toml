[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"
watchPatterns = ["src/**", "index.html", "package.json", "vite.config.ts"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
